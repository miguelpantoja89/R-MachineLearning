 #### Comandos utilies
### Ctrl+L para limpiar la consola

#Instalar paquetes
##install.packages("plotly")
### Para llamar al paquete 
print("hello")
##library(plot)
help(print)
x <- 9.45
y <- (4*x) - log(x) + cos(2*pi*x)

#Eliminar una variable 
rm(x)

#Eliminar contenido del WKspace
rm(list = ls())
